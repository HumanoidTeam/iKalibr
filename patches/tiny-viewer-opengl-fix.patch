--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -36,6 +36,14 @@
 
 find_package(Eigen3 REQUIRED)
 find_package(PCL REQUIRED)
+# Create OpenGL target aliases for Pangolin compatibility
+find_package(OpenGL REQUIRED COMPONENTS OpenGL)
+if(NOT TARGET OpenGL::OpenGL AND TARGET OpenGL::GL)
+    add_library(OpenGL::OpenGL ALIAS OpenGL::GL)
+endif()
+if(NOT TARGET OpenGL::EGL AND TARGET OpenGL::GL)
+    add_library(OpenGL::EGL ALIAS OpenGL::GL)
+endif()
 find_package(Pangolin REQUIRED)
 
 aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR}/src/core CORE_SRC_FILES)
